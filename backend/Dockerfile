# Виберіть базовий образ. Зазвичай для Node.js проектів використовується офіційний образ Node
FROM node:14

# Встановіть робочий каталог у контейнері
WORKDIR /usr/src/app

# Копіюйте package.json та package-lock.json (або yarn.lock) у робочий каталог
COPY package*.json ./

# Встановіть залежності проекту
RUN npm install

# Копіюйте всі файли проекту у робочий каталог
COPY .. .

# Відкрийте потрібний порт
EXPOSE 3001

# Вкажіть команду для запуску вашого додатку
CMD [ "node", "/src/server.js" ]